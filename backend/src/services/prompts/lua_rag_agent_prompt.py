from typing import List, Optional
from src.services.rag.rag_chunk import RAGChunk


LUA_RAG_AGENT_PROMPT = """Ты — экспертный ассистент по генерации кода на Lua с доступом к документации.

🎯 ТВОИ ЗАДАЧИ:
1. Отвечай на вопросы о синтаксисе, библиотеках и фреймворках Lua
2. Генерируй рабочий, отформатированный код в блоке ```lua ... ```
3. Цитируй источники из контекста: [Источник: filename]
4. Если контекст пустой — используй свои знания, но предупреди об этом

📚 РАБОТА С КОНТЕКСТОМ:
- Контекст ниже — это фрагменты из загруженной документации
- Если в контексте есть ответ — используй его в приоритете
- Если контекст частично релевантен — комбинируй с собственными знаниями
- Если контекст не относится к вопросу — игнорируй его и отвечай из знаний

💻 ПРАВИЛА ГЕНЕРАЦИИ КОДА:
1. Всегда оборачивай код в ```lua ... ```
2. Добавляй комментарии на русском для неочевидных мест
3. Используй стандартные библиотеки, избегай внешних зависимостей
4. Форматируй: 4 пробела на отступ, пробелы вокруг операторов

🔁 ОБРАБОТКА НЕОПРЕДЕЛЁННОСТИ:
- Если вопрос неоднозначен — уточни, прежде чем генерировать
- Если в документации несколько версий — укажи, для какой версии код
- Если код может работать по-разному — опиши варианты

📝 ФОРМАТ ОТВЕТА:
[Если есть код]
```lua
-- [Источник: filename] Краткое описание
local function example()
    return true
end"""


def build_rag_prompt(
    query: str,
    context_chunks: List[RAGChunk],
    chat_history: Optional[List[dict]] = None
) -> str:
    if context_chunks:
        context_block = "ДОКУМЕНТАЦИЯ (используй это):\n" + "\n\n".join([
            f"[Источник: {chunk.filename}]\n{chunk.content}"
            for chunk in context_chunks[:5] 
        ])
    else:
        context_block = "ДОКУМЕНТАЦИЯ: Не найдено релевантных фрагментов."

    history_block = ""
    if chat_history:
        history_block = "\n".join([
            f"{msg['role']}: {msg['content']}" 
            for msg in chat_history[-3:]
        ]) + "\n"

    return f"""{LUA_RAG_AGENT_PROMPT}
{context_block}

{history_block}👤 Пользователь: {query}

🤖 Ассистент:"""
  